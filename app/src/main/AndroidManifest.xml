<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chess05.chess">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayBoard" />
        <activity android:name=".PastGame" />
        <activity android:name=".FinishedGame" />
        <activity android:name=".SaveGame" />
        <activity android:name=".InputGameName" />
        <activity android:name=".QuitDecision" />
        <activity
            android:name=".SortButtons"
            android:label="@string/title_activity_sort_buttons"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".WalkthroughGame" />
    </application>

</manifest>